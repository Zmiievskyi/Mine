   services:
     db:
       image: postgres:16
       environment:
         POSTGRES_DB: minegnk
         POSTGRES_USER: minegnk
         POSTGRES_PASSWORD: minegnk
       ports:
         - "5432:5432"
       volumes:
         - db-data:/var/lib/postgresql/data
   volumes:
     db-data: